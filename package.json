{
  "name": "flexit-docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && docusaurus start",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && docusaurus build",
    "swizzle": "set NODE_OPTIONS=--openssl-legacy-provider && docusaurus swizzle",
    "deploy": "set NODE_OPTIONS=--openssl-legacy-provider && docusaurus deploy"
  },
  "dependencies": {
    "@docusaurus/core": "2.0.0-alpha.66",
    "@docusaurus/preset-classic": "2.0.0-alpha.66",
    "classnames": "^2.2.6",
    "plugin-image-zoom": "github:flexanalytics/plugin-image-zoom",
    "react": "^16.8.4",
    "react-dom": "^16.8.4"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
